#include <iostream>
#include <list>
using namespace std;

int main(){
    int i;
    list <int> intV(5); // 이렇게 선언을 하고 해줘야 함 (이 줄을 주석처리 해보고 작동해보자, 오류는 안뜨지만 입력이 안됨)
    // 음 윗줄 주석처리 하면 애초에 에러가 뜨는데 무슨 얘기를 하고자 하는지 고민해봐야 할듯
    list<int>::iterator it;

    cout << "정수값 5개 입력: " ;
    for(it=intV.begin(); it != intV.end(); it++){
        cin >> *it;
    }
    cout << "정수값 5개 출력: ";
    for(it=intV.begin(); it != intV.end(); it++){
        cout << *it << "\t";
    }
    cout << endl;
}

/*
여기서는 일반 포인터 변수가 아니라, 이터레이터를 사용해서 구현한 방법이다.
교수님이 말씀하길, 리스트에서는 어떤걸 사용못한다고 했던거 같음
*** 이터레이터랑 일반 포인터 변수의 차이점을 눈여겨 보길
*/

/*
이 연습에서는 이터레이터를 활용해서 동일한 구동을 구현해놨음
*/

//vector 는 클래스 t 타입임을 기억해야 한다.
