#include <iostream>
#include <vector>
using namespace std;

void printVector(vector<int> v){
    for(int i=0; i<v.size(); i++){
        cout << v.at(i) << " " ;
    }
    cout << endl;
}

int getAverage(vector<int> v){
    int result=0;
    for(int i=0; i<v.size(); i++){
        result += v.at(i);
    }
    result /= v.size();
    return result;
}

int main(){
    vector<int> v; //벡터 객체 생성 
    cout << "정수를 5개 입력하세요>>";
    for(int i=0; i<5; i++){
        int num;
        cin >> num;
        v.push_back(num);
    }
    printVector(v);
    cout << "평균= " << getAverage(v) << endl;    
}


/*
내가한 코드는 잘못된 것임
vector를 인자로 사용할 경우, 반드시 참조의 형태로 사용해야 한다! 11:50 사진에서 정답 확인할 것

vector도 일종의 객체이므로, 객체를 전달 할 때는 해당 객체를 카피해 와야 한다는거 같은데
정확히 왜 vector는 참조의 형태로 가져와야 하는가 공부할 것
*/
